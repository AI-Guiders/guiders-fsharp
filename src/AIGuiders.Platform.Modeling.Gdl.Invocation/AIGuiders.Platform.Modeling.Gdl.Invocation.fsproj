<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>

    <TargetFramework>net10.0</TargetFramework>

    <RootNamespace>AIGuiders.Platform.Modeling.Gdl.Invocation</RootNamespace>

    <AssemblyName>AIGuiders.Platform.Modeling.Gdl.Invocation</AssemblyName>

    <Description>GDL invocation modeling: normalized command line, engage kind, line phase, arg mechanic (IR.Invocation port).</Description>

    <PackageId>AIGuiders.Platform.Modeling.Gdl.Invocation</PackageId>

  </PropertyGroup>

  <ItemGroup>

    <Compile Include="InvocationModel.fs" />

    <Compile Include="InvocationNotation.fs" />

  </ItemGroup>

</Project>