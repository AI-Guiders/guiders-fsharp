<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>

    <TargetFramework>net10.0</TargetFramework>

    <RootNamespace>AIGuiders.Platform.Modeling.Agent</RootNamespace>

    <AssemblyName>AIGuiders.Platform.Modeling.Agent</AssemblyName>

    <Description>Agent modeling: response envelope, detail tier, next hints (IR.Agent port; IntentOutcome stays execution-side).</Description>

    <PackageId>AIGuiders.Platform.Modeling.Agent</PackageId>

  </PropertyGroup>

  <ItemGroup>

    <Compile Include="AgentModel.fs" />

  </ItemGroup>

</Project>
