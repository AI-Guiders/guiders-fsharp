<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>

    <TargetFramework>net10.0</TargetFramework>

    <RootNamespace>AIGuiders.Platform.Modeling.Notations.Keyboard</RootNamespace>

    <AssemblyName>AIGuiders.Platform.Modeling.Notations.Keyboard</AssemblyName>

    <Description>Keyboard notation wire model: normalized key sequences, chord steps (Neutral port; normalizer stays execution-side).</Description>

    <PackageId>AIGuiders.Platform.Modeling.Notations.Keyboard</PackageId>

  </PropertyGroup>

  <ItemGroup>

    <Compile Include="KeyboardModel.fs" />

  </ItemGroup>

</Project>