<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>

    <TargetFramework>net10.0</TargetFramework>

    <RootNamespace>AIGuiders.Platform.Modeling.Gdl.Command</RootNamespace>

    <AssemblyName>AIGuiders.Platform.Modeling.Gdl.Command</AssemblyName>

    <Description>GDL command modeling: descriptor, arg-tail policy, catalog route entry (IR.Command port).</Description>

    <PackageId>AIGuiders.Platform.Modeling.Gdl.Command</PackageId>

  </PropertyGroup>

  <ItemGroup>

    <Compile Include="CommandModel.fs" />

    <Compile Include="CommandArgTailPolicy.fs" />

    <Compile Include="CatalogRouteEntry.fs" />

  </ItemGroup>

</Project>